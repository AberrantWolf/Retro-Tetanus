import { TabWidget , VerticalBox, GroupBox, LineEdit, Button} from "std-widgets.slint";
import { Constants } from "constants.slint";

export component ScrapeTabView inherits Rectangle {
	background: green;
	VerticalBox {
		Text {
			text: "Placeholder B";
		}
		
		// Filler rectangle
		Rectangle {
			background: darkgreen;
		}
	}
}
